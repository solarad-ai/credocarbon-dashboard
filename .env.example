# CredoCarbon Environment Variables
# Copy to .env and fill in values

# ==========================================
# Core Settings
# ==========================================
ENV=local                              # local | dev | staging | prod
DEBUG=true                             # Enable debug logging
SECRET_KEY=your-secret-key-here        # JWT signing key (generate with: openssl rand -hex 32)

# ==========================================
# Database (PostgreSQL)
# ==========================================
DATABASE_URL=postgresql://credo:credo_password@localhost:5432/credo_carbon

# For Docker Compose, use:
# DATABASE_URL=postgresql://credo:credo_password@db:5432/credo_carbon

# For Supabase:
# DATABASE_URL=postgresql://postgres.[project]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ==========================================
# Cloud Provider Selection
# ==========================================
CLOUD_PROVIDER=local                   # local | gcp | aws | azure

# Per-service overrides (optional)
# STORAGE_BACKEND=gcp                  # Override file storage only
# EVENT_BACKEND=local                  # Override event bus only
# QUEUE_BACKEND=local                  # Override task queue only
# EMAIL_BACKEND=local                  # Override email only

# ==========================================
# GCP Configuration (when CLOUD_PROVIDER=gcp)
# ==========================================
GCP_PROJECT_ID=your-project-id
GCS_BUCKET_NAME=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json  # Local dev only

# Cloud Run deployment
CLOUD_TASKS_LOCATION=asia-south2
CLOUD_TASKS_QUEUE=default
CLOUD_TASKS_TARGET_URL=https://your-api.run.app

# ==========================================
# AWS Configuration (when CLOUD_PROVIDER=aws)
# ==========================================
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_SQS_QUEUE_URL=https://sqs.region.amazonaws.com/account/queue
# AWS_SNS_TOPIC_ARN=arn:aws:sns:region:account:topic

# ==========================================
# Azure Configuration (when CLOUD_PROVIDER=azure)
# ==========================================
# AZURE_STORAGE_ACCOUNT=your-storage-account
# AZURE_STORAGE_KEY=your-storage-key
# AZURE_CONTAINER_NAME=your-container
# AZURE_SERVICE_BUS_CONNECTION=your-connection-string

# ==========================================
# Email Configuration
# ==========================================
EMAIL_FROM=noreply@credocarbon.com
SENDGRID_API_KEY=SG.your-sendgrid-api-key

# For local dev with MailHog
USE_MAILHOG=false
MAILHOG_HOST=localhost
MAILHOG_PORT=1025

# ==========================================
# CORS Configuration
# ==========================================
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# For production:
# CORS_ORIGINS=https://your-frontend-domain.com

# ==========================================
# Frontend Configuration
# ==========================================
NEXT_PUBLIC_API_URL=http://localhost:8080

# For production:
# NEXT_PUBLIC_API_URL=https://your-api-domain.com

# ==========================================
# Docker Compose (optional overrides)
# ==========================================
POSTGRES_USER=credo
POSTGRES_PASSWORD=credo_password
POSTGRES_DB=credo_carbon
DB_PORT=5432
REDIS_PORT=6379
API_PORT=8080
WEB_PORT=3000
